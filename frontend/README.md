# Frontend — screens, Flutter, website. No SQL, no OTP send, no matching.

- `mobile/` — SaHayak Android/iOS (Flutter). Home: Need blood | I can donate.
- `web/` — later site/console UI on the same `/v1`.

Talk only to middleware (`http://127.0.0.1:8080/v1`).
