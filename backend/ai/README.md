# Backend AI (Phase 3). LangGraph lives in middleware `app/graph` behind POST /v1/assistant/messages.
# Flutter never calls this folder. Default: rules nodes, no paid LLM. Optional Groq if SAHAYAK_GROQ_KEY and flags.ai_enabled.
# Knowledge: local markdown until pgvector. MCP / Deep Agents: not used.
